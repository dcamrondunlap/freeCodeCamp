//this is just an example since i cant get the the full json from the video

//import data from "./products.json"

//export default defineEventHandler(() => {
//  return {
//    data,
//  };
//});


//when using useLazyFetch need to return a promise

//export default defineEventHandler(() => {
//  return new Promise <any>((resolve) => {
//    setTimeOut(() => {
//      resolve(data);
//    }, 2000
//  });
//});
